import React from 'react'

const buttonStyle = {
    padding: '10px 20px'
};

const Button = (props) => {
    return (
        <div>change again</div>
    )
}

Button.defaultProps = {
    onClick: () => { },
    label: ''
};

export default Button;